# HCM Optional Generators

This repository contains non-essential, optional extensions
that may generate *possibilities*, not artifacts.

Nothing here:
- creates value
- triggers release
- causes publication
- interacts with payment channels

All outputs are considered:
latent, incomplete, and non-binding.

This repository may be ignored forever without loss.
